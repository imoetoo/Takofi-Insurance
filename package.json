{
  "name": "solidity-learn",
  "version": "1.0.0",
  "scripts": {
    "dev"           : "vite",
    "build"         : "vite build",
    "preview"       : "vite preview",
    "compile"       : "hardhat compile",
    "clean"         : "hardhat clean",
    "node"          : "hardhat node --hostname 127.0.0.1 --port 8545",
    "deploy:all"    : "hardhat run scripts/deployAll.js --network localhost",
    "mint"          : "hardhat run --network localhost scripts/mintTokensToAccount.js",
    "addresses"     : "hardhat run --network localhost scripts/getTokenAddresses.js",
    "bal"           : "hardhat run --network localhost scripts/analyzeBalances.js"    
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "hardhat": "^2.26.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "node": "^22.17.1",
    "typescript": "^5.8.3",
    "viem": "^2.33.0",
    "vite": "^7.0.5"
  }
}
